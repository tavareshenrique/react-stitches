import { styled } from '../../styles/theme';

export const Container = styled('div', {
  maxWidth: '1120px',
  margin: '0 auto',
  padding: '10rem 2rem',
  height: '100vh',

  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});
